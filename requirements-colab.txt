# Minimal install for Google Colab (avoids reinstalling preinstalled packages)
# Install with: pip install -r requirements-colab.txt -q
#
# Colab already has: torch, torchvision, numpy, Pillow, pandas, tqdm, matplotlib

# Core ML
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.30.0

# Geospatial
rasterio>=1.3.9
Pillow>=10.0.0
opencv-python-headless>=4.9.0

# Config / utils
pyyaml>=6.0
scikit-learn>=1.4.0

# API (optional)
fastapi>=0.111.0
uvicorn>=0.29.0

# Testing
pytest>=8.0.0
